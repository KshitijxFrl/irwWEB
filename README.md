# IRONWILL V1 Frontend

Plain HTML, CSS, and JavaScript frontend for the IRONWILL V1 project report and model demo.

Open `index.html` in a browser to view it.

The current demo is static. It prepares local `ironwillV1_infer` commands. A backend can be added later to run inference from the browser.
# irwWEB
